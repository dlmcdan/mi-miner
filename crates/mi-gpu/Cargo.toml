[package]
name = "mi-gpu"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
mi-core = { path = "../mi-core" }
tracing = "0.1"
hex = "0.4"

[target.'cfg(target_os = "macos")'.dependencies]
metal = "0.29"
objc = "0.2"
block = "0.1"

[build-dependencies]
