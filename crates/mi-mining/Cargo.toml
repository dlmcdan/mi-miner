[package]
name = "mi-mining"
version = "0.1.0"
edition = "2021"

[dependencies]
mi-core = { path = "../mi-core" }
sha2 = { version = "0.10", features = ["asm", "compress"] }
tracing = "0.1"
num_cpus = "1"
hex = "0.4"
